<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MeetingApp.Models.ViewModels"
             x:Class="MeetingApp.Pages.AddMeetingPage"
             x:DataType="vm:MeetingDetailViewModel"
             Title="Detail Schůzky">

    <!--<ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Label Text="Název schůzky"/>
            <Entry Text="{Binding SelectedMeeting.Title}" Placeholder="Zadejte název"/>

            <Label Text="Datum"/>
            <DatePicker Date="{Binding SelectedMeeting.Date}"/>

            <Label Text="Začátek"/>
            <TimePicker Time="{Binding SelectedMeeting.StartTime}"/>

            <Label Text="Konec"/>
            <TimePicker Time="{Binding SelectedMeeting.EndTime}"/>

            <Label Text="Opakování"/>
            <Picker ItemsSource="{Binding RecurrencePatterns}" 
                    SelectedItem="{Binding SelectedMeeting.Recurrence.Pattern}" />

            <Label Text="Do kdy opakovat"/>
            <DatePicker Date="{Binding SelectedMeeting.Recurrence.EndDate}" />

            --><!-- Přidání účastníka --><!--
            <Label Text="Přidat účastníka"/>
            <Picker ItemsSource="{Binding AllUsers}"
                    ItemDisplayBinding="{Binding FullName}"
                    SelectedItem="{Binding SelectedUserToAdd}"/>
            <Button Text="Přidat" Command="{Binding AddParticipantCommand}" CommandParameter="{Binding SelectedUserToAdd}" />

            --><!-- Účastníci --><!--
            <Label Text="Účastníci" FontAttributes="Bold"/>
            <CollectionView ItemsSource="{Binding Participants}" HeightRequest="150">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="5" ColumnDefinitions="*,Auto">
                            <Label Text="{Binding FullName}" FontSize="14"/>
                            <Button Text="🗑" Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MeetingDetailViewModel}}, Path=RemoveParticipantCommand}" CommandParameter="{Binding .}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="Uložit změny" Command="{Binding SaveChangesCommand}"/>
            <Button Text="Smazat schůzku" Command="{Binding DeleteMeetingCommand}" BackgroundColor="DarkRed" TextColor="White"/>

        </VerticalStackLayout>
    </ScrollView>-->
</ContentPage>
